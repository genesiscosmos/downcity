---
title: ImagePreview
description: A controlled full-screen image preview
---

# ImagePreview

Pass a data URL, Blob URL, or remote URL. The component never reads local files or uses host bridges.

```tsx
<ImagePreview open={open} onOpenChange={set_open} src={image_url} alt="Preview" />
```

---
title: Plugin 总览
description: 理解 City Plugin 的所有权、能力、上下文与 Session Hook。
---

# Plugin 总览

Plugin 是 Downcity 的统一扩展产品单元，可以提供 Action、Hook、Resolve、System、Availability、
HTTP Route、生命周期以及可选的 Mainview/Config。

Plugin 不属于 Agent。City 持有 catalog、main、Plugin 唯一实例与完整生命周期；全部 Plugin 自动
提供给全部 Agent，Session 在执行检查点取得稳定的 Hook 视图。

## SDK 装配

```ts
import { Agent } from "@downcity/agent";
import { City } from "@downcity/city";
import { create_builtin_plugin_registrations } from "@downcity/plugins";

const registrations = create_builtin_plugin_registrations();
const city = new City({ workspaces: [workspace], plugins: registrations });
const agent = new Agent({ id: "repo-helper", model });

city.agents.add(agent);
```

`CityOptions.plugins` 或 `city.plugins.add()` 负责登记；`city.agents.add()` 只登记 Agent。

## PluginContext

Action、Hook、System 与 Availability 都使用当前作用域的 `PluginContext`。它提供受限的：

- `city`：Embassy 与 Plugin 间协作端口。
- `agent`：身份、指令和 Session 集合。
- `workspace`：文件、Shell、env 与稳定 ID。
- `session` / `turn`：当前调用存在 Session/Turn 时的直接通信句柄。
- `config`：City 为当前 Agent/Plugin 作用域投影的只读配置。
- `storage`：当前 Agent/Plugin 私有存储。

对象句柄用于进程内直接通信，ID 用于稳定身份和序列化；Plugin 不需要只拿 `session_id` 再绕回宿主查询。

## 唯一实例与配置投影

一个 City 中每个 Plugin ID 只有一个实例。`start/stop` 属于 City 实例，`connect/disconnect`
属于 Agent/Workspace scope。宿主仍可保存 Profile，但它只用于按 Agent 投影 `context.config`，
不会创建额外 Plugin 实例。Memory 和 Chat 等 Plugin 必须在同一实例内按 Context 隔离作用域。

## 推荐阅读

1. [Plugin Lifecycle](/zh/agent-sdk-docs/plugins/lifecycle)
2. [Plugin Actions](/zh/agent-sdk-docs/plugins/plugin-actions)
3. [Plugin Hooks](/zh/agent-sdk-docs/plugins/plugin-hooks)
4. [Plugin 配置](/zh/agent-sdk-docs/plugins/configuration)
5. [Plugin 开发指南](/zh/agent-sdk-docs/plugins/plugin-development)
6. [内建 Plugin 手册](/zh/plugins-docs)

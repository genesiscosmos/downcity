---
title: 例子：最小本地 Agent
description: 一个最小可运行的本地 Agent SDK 示例
---

# 例子：最小本地 Agent

```ts
import { Agent } from "@downcity/agent";
import { Workspace } from "@downcity/workspace";

const agent = new Agent({
  id: "repo-helper",
  tools: {},
});
const agent_workspace = agent.enter(new Workspace({
  id: "project",
  path: "/path/to/project",
}));
```

然后：

```ts
const session = await agent_workspace.sessions.create();
await session.set({ model });
const turn = await session.prompt({ query: "继续" });
await turn.finished;
```

这条最小例子适合先验证 3 件事：

- Agent 能不能成功构造
- Session 能不能成功创建
- 模型能不能成功执行

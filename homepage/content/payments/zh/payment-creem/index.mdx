---
title: Creem Payment Provider
---

将 `creemPaymentProvider()` 放入 `PaymentService.providers`，配置 `CREEM_API_KEY`、`CREEM_PRODUCT_ID` 和可选的 `CREEM_WEBHOOK_SECRET`。用户创建订单：

```ts
await city.payment.method("creem").invoke({
  credits: 5_000_000,
  amount_minor: 500,
  idempotency_key: "order_123",
});
```

Payment 通过 `on_paid(record)` 通知接入方，接入方使用 `payment:${record.payment_id}` 幂等调用 `credits.topup()`。

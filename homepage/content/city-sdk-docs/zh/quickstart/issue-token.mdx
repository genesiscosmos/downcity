---
title: 签发 User Token
description: 通过 Embassy Admin 请求 Federation 签发用户凭证。
---

```ts
import { Embassy } from "@downcity/federation";

const embassy = new Embassy({
  federation_url: "https://fed.example.com",
  admin_token: administrator_session_token,
});

const issued = await embassy.admin.service("accounts")
  .action("tokens/issue")
  .invoke({
    bureau_id: "product-web",
    user_id: "user_123",
    metadata: { plan: "pro" },
    ttl: "7d",
  });
```

Token 签发由 Federation 执行。Bureau 负责验证 User Token，但不签发它。

---
title: Agent CLI
description: 如何使用 CLI 管理 Agent 项目
---

# Agent CLI

`city` CLI 是 Agent SDK 的本地宿主。它从全局 DB 读取 Agent 配置，将模型和 Plugin 传给 Agent SDK，并由唯一 CLI City 管理长期运行生命周期。

## 项目初始化

```bash
city agent create
```

不传路径时，可以选择当前目录或通过系统原生窗口选择其他文件夹；也可以显式传入相对路径或绝对路径，例如 `city agent create .`、`city agent create ./my-project`。

这会生成：

- `.agents/skills/`
- `.downcity/`
- `.env`
- CLI 全局 DB 中的 Agent 配置

## 项目目录结构

```text
my-project/
├── .env
├── .agents/skills/
├── .downcity/          # 仅运行数据
└── 你的业务代码
```

## 关键命令

- `city` — 直接打开 Agent 列表；Federation 与全局 Plugin 操作位于同页的“设置”分组
- `city on` — 启动 CLI City，并恢复全部已绑定 Agent
- `city status` — 查看 City、HTTP/RPC 端口与已配置 Agent
- `city restart` — 重启 City 并重新加载配置
- `city off` — 停止 CLI City
- `city agent chat [agent_id]` — 与 Agent 对话；City 未开启时也可使用

## Chat TUI 安全策略

从 Agent 列表直接进入最近对话后，使用 `/security` 打开当前 Session 的安全策略选择器：

- `Default` — 对应 Session 的 `ask` 模式；每次 unrestricted Shell 操作都需要明确审批。
- `Always Allow` — 对应 `always-allow` 模式；当前 Session 后续的 unrestricted Shell 操作会自动批准。

当前策略展示在 Chat Header 中。策略只影响当前 Session 的后续 Shell 请求；已经等待审批的请求仍需单独处理。`Always Allow` 不会绕过危险命令拒绝、Workspace 边界或其他运行时安全检查。

## CLI 做了什么

1. 从 CLI 全局 DB 读取 Agent 配置
2. 通过已连接的 Federation AIService 解析 `execution.modelId`
3. 将解析后的配置传给 `new Agent(...)`
4. CLI 从 Repository 读取配置并显式创建 Agent，再注册到 `new City(agents)`
5. 在一个 HTTP 端口和一个 RPC 端口上按 `agent_id` 暴露 Agent

## CLI vs SDK

CLI 用于日常项目管理。SDK 用于将 Agent 能力嵌入自己的应用。两者使用相同的执行核心。

继续阅读：

- [Agent SDK](/zh/docs/agent/sdk)
- [Agent 生命周期](/zh/docs/agent/lifecycle)

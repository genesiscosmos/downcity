# Skeleton

保持异步内容加载时的结构稳定。

## Preview

<ComponentDemo component_id="skeleton" />

## Usage

```tsx
import { Skeleton } from "@downcity/ui";

export function Example() {
  return <Skeleton className="h-24 w-full rounded-xl" />;
}
```

Skeleton 应尽量接近最终内容结构，减少布局跳动。

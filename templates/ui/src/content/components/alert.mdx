# Alert

呈现内联状态、提醒和阻断信息。

## Preview

<ComponentDemo component_id="alert" />

## Usage

```tsx
import { Alert, AlertDescription, AlertTitle } from "@downcity/ui";

export function Example() {
  return <Alert><div><AlertTitle>Saved</AlertTitle><AlertDescription>Your changes are ready.</AlertDescription></div></Alert>;
}
```

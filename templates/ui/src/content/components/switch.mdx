# Switch

切换一个布尔配置。

## Preview

<ComponentDemo component_id="switch" />

## Usage

```tsx
import { Switch } from "@downcity/ui";

export function Example() {
  return <Switch defaultChecked aria-label="Enable live preview" />;
}
```

没有可见 Label 时必须提供 `aria-label`。

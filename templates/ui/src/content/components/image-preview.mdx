# ImagePreview

由宿主提供图片 URL 或 data URL 的受控全屏预览，不依赖本地资源能力。

<ComponentDemo component_id="image-preview" />

## Usage

```tsx
<ImagePreview open={open} onOpenChange={set_open} src={image_url} alt="Preview" />
```

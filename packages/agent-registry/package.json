{
  "name": "@downcity/agent-registry",
  "version": "0.1.1",
  "type": "module",
  "description": "Downcity CLI 与桌面客户端共享的 Agent 注册仓储。",
  "main": "./bin/index.js",
  "types": "./bin/index.d.ts",
  "files": [
    "bin/",
    "package.json",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "test": "pnpm build && node --test test/*.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
